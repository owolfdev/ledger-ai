# Blog

Type a `search` and a topic to search for blog posts. Use command like `latest`, `popular`, `search`, `list` to find posts. To open one, type `go` and then any unique portion of the post title. Type `help` for more information on these commands. Type `update` to clear the terminal and see the latest posts.
