import { InfoIcon } from "lucide-react";

# Protected Page

<span>
  <InfoIcon size="16" strokeWidth={2} /> This is a protected page that you can
  only see as an authenticated user.
</span>

Type `go projects` or `go blog` to dive in.
